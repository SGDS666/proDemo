import React from 'react';
import PageIndex from '@/components/Global/PageIndex';

const Notifications: React.FC = () => {
  return <PageIndex pageGroup="notifications" />;
};

export default Notifications;
